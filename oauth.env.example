# Environment configuration for the OAuth example runner
# Copy to .env and provide your own values before running ExampleWithOAuth
GOOGLE_CLIENT_SECRETS_PATH=/client_secrets.json
YOUTUBE_CHANNEL_ID=@BrunoGroeningOrg
